# Opauth test application

This is an unstable repository and should be treated as an alpha.

## Installation

1. Clone this repository
2. Install with composer:
```bash
composer install
```

## Configuration

Add your Opauth configs to `config/app.php`